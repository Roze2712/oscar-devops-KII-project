apiVersion: v1
kind: Service
metadata:
  name: oscar-app
  labels:
    app: oscar-app
spec:
  type: LoadBalancer
  selector:
    app: oscar-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
